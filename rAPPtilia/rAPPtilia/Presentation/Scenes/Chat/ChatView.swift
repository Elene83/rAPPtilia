import SwiftUI

struct ChatView: View {
    let coordinator: MainCoordinator
    
    var body: some View {
        VStack {
            Text("Chat")
        }
        .navigationTitle("Chat")
    }
}
